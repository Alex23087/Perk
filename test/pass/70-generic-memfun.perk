import "stdio.h"

type <T> List = Nil | Cons(int, List @ T)

// fun <T> List.forall (self: List @ T, pred: (int) => bool): bool {
//     match (self) {
//         Nil {
//             return true
//         },
//         Cons (var hd, var tl) {
//             if (pred(hd)) {
//                 return tl.forall(pred)
//             } else {
//                 return false
//             }
//         }
//     }
// }

type_fun <T> List.make_empty(): List @ T {
    return Nil;
}

fun main() : void {
    printf("%s", if
        List@int.make_empty().forall(
            (a: int): bool {
                return false
            }
        ) then "success" else "failure"
    );
}