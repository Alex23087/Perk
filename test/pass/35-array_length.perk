import "stdio.h"

fun main(): void {
    let arr: [int] = [5, 4, 3, 2, 1]; // Initialize an array with values
    printf("Array elements: ");
    for (let i: int = 0; i < arr.length; i++) {
        if (i > 0) {
            printf(", ");
        };
        printf("%d", arr[i]);
    }
}